version: '3'

services:
  httpd:
    image: abiosoft/caddy
    volumes:
      - ./Caddyfile:/etc/Caddyfile
      - ./client/public:/var/www/public
    ports:
      - "27022:27022"
    links:
      - server
    restart: always

  server:
    build: .
    command: gunicorn -b 0.0.0.0:27022 -w4 main:app --access-logfile - --error-logfile -
    volumes:
      - ./server:/srv/django
    restart: always
